# Pinned deliberately. FabricOps drives two libraries whose behaviour it depends on in
# detail - fabric-cicd's parameterisation and the Fabric CLI's command surface - and an
# unpinned CI run picking up a new major of either is a broken deployment, not a warning.
# Bump these on purpose, with the test suite as the gate.

requests==2.33.1
azure-identity==1.25.3
ruamel.yaml==0.19.1
fabric-cicd==1.3.0
ms-fabric-cli==1.5.0
PyNaCl==1.6.2
